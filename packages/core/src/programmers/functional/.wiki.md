# Functional Programmer Migration Notes

The TypeScript implementation wraps function parameters, return values, or both
depending on the selected functional programmer. The Go backend preserves those
entrypoints as namespace-like variables and emits JavaScript wrappers for
assert, is, and validate modes.

Without checker-level function declaration metadata, the Go output keeps the
runtime call shape and error-result contract while using definedness checks as
the available portable guard.
