syntax = "proto3";

message TagStep {
    repeated TagStep.Type value = 1;
    message Type {
        double exclusiveMinimum = 1;
        double minimum = 2;
        double range = 3;
        double multipleOf = 4;
    }
}