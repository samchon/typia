# Notation Programmer Migration Notes

The TypeScript implementation recursively rewrites object keys according to the
requested naming convention and composes that converter with assert, is, and
validate programmers. The Go backend mirrors those entrypoints with a shared
recursive converter for camel, pascal, and snake modes.

The emitted wrappers use the shared predicate helpers for validation before
returning converted data, null, or validation results.
