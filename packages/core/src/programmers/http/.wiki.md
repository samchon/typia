# HTTP Programmer Migration Notes

The TypeScript implementation decomposes each HTTP feature into a decoder and
validation wrapper. The Go backend follows the same split with shared query,
headers, form-data, and parameter decoders, then assert, is, and validate
wrappers over the decoded value.

The Go backend emits JavaScript source strings, so checker-derived metadata is
approximated through the available type text and the shared predicate helpers.
