name: build
on: 
  pull_request:
    paths:
      - '.github/workflows/build.yml'
      - 'config/**'
      - 'packages/**'
      # `packages/{langchain,mcp}`'s build shells out to
      # `scripts/create-conditional-declarations.cjs`, so this lane is the one
      # that runs it. It used to be claimed by `nestia.yml`, which triggered on
      # `scripts/**` and ran `pnpm test` -- a command that never loads it.
      - 'scripts/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'package.json'
jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 #v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 #v4
        with:
          node-version: 24.x
      - uses: actions/setup-go@v5
        with:
          go-version: 1.26.x
      - uses: pnpm/action-setup@v4
      
      - name: Install Dependencies
        run: pnpm install

      - name: Build Packages
        run: pnpm run build
