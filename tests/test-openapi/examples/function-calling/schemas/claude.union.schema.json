{
  "description": "Current Type: {@link IDrawProps}",
  "type": "object",
  "properties": {
    "shape": {
      "oneOf": [
        {
          "$ref": "#/$defs/IRectangle"
        },
        {
          "$ref": "#/$defs/ICircle"
        },
        {
          "$ref": "#/$defs/IPolygon"
        }
      ],
      "discriminator": {
        "propertyName": "type",
        "mapping": {
          "rectangle": "#/$defs/IRectangle",
          "circle": "#/$defs/ICircle",
          "polygon": "#/$defs/IPolygon"
        }
      }
    }
  },
  "required": [
    "shape"
  ],
  "additionalProperties": false,
  "$defs": {
    "IRectangle": {
      "type": "object",
      "properties": {
        "type": {
          "const": "rectangle"
        },
        "p1": {
          "$ref": "#/$defs/IPoint"
        },
        "p2": {
          "$ref": "#/$defs/IPoint"
        }
      },
      "required": [
        "type",
        "p1",
        "p2"
      ]
    },
    "IPoint": {
      "type": "object",
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ]
    },
    "ICircle": {
      "type": "object",
      "properties": {
        "type": {
          "const": "circle"
        },
        "radius": {
          "type": "number"
        },
        "center": {
          "$ref": "#/$defs/IPoint"
        }
      },
      "required": [
        "type",
        "radius",
        "center"
      ]
    },
    "IPolygon": {
      "type": "object",
      "properties": {
        "type": {
          "const": "polygon"
        },
        "inner": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "type": "array",
              "items": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/IPoint"
                }
              }
            }
          ]
        },
        "outer": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/IPoint"
          }
        }
      },
      "required": [
        "type",
        "inner",
        "outer"
      ]
    }
  }
}